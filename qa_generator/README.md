# qa-generator

modified copy from graph-framework/
this auto-generates query and regex with the pattern generator llm and checks the regex on a logfile

this graph is used to generate few shot examples for the pattern_generator_prompt.txt

# imports

`uv add langchain langchain-ollama langchain-chroma langgraph`